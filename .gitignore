# Ambiente virtual
.venv/
venv/
__pycache__/
*.py[cod]

# Next.js / Node
node_modules/
.next/
out/
*.tsbuildinfo
# `.env*` e nao `.env*.local`: o padrao antigo nao pegava copias de seguranca
# como `.env.local.bak`, e foi assim que uma chave real entrou num commit.
.env*
!.env.local.example

# Dados gerados pelo notebook (reproduziveis a partir do CSV bruto)
data/processed/

# Jupyter
.ipynb_checkpoints/

# Respostas cruas da API do Spotify (cache retomavel)
data/raw_api/

# Logs de execucao
logs/

# Dumps do MusicBrainz (varios GB, baixados sob demanda)
data/musicbrainz/

# Dataset GTZAN (audio, ~1.2 GB)
data/gtzan/

# Binario WASM copiado da essentia.js no pre-build
public/essentia/

# K=11 / Q9 drop-one: arrays de log-likelihood por sample (~2.7 GB, regeneráveis)
q9_*.npy

# Q9 posteriors NetCDF (~1.1 GB, regeneráveis rodando o notebook)
q9_*.nc

# K=11 pipeline (scripts/k11_pipeline/): artefatos e logs gerados
# (o parquet e copia do data/processed/ original; os artefatos .nc/.json sao outputs)
scripts/k11_pipeline/artifacts/
scripts/k11_pipeline/spotify_tracks_limpo.parquet
scripts/k11_pipeline/*.log

# Saida bruta do antigo scripts/train_k11.py (removido; ver scripts/k11_pipeline/).
# Mantida localmente como backup do treino ja rodado; nao versionada (~250 MB).
artifacts/k11_posterior.nc
artifacts/split_indices.npz
